{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    "@semantic-release/npm",
    [
      "@semantic-release/git",
      {
        "assets": ["CHANGELOG.md", "package.json", "package-lock.json"],
        "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
      }
    ],
    [
      "@semantic-release/github",
      {
        "assets": [
          {
            "path": "dist/**/*",
            "label": "Distribution files"
          },
          {
            "path": "package.json",
            "label": "Package manifest"
          }
        ],
        "successComment": ":tada: This ${issue.pull_request ? 'pull request' : 'issue'} is included in version ${nextRelease.version} :tada:\n\nThe release is available on [GitHub release](${releases.filter(release => release.name === 'GitHub release')[0].url}) and [npm](https://www.npmjs.com/package/${name})",
        "successCommentCondition": "<%= issue.user.type !== 'Bot' %>",
        "releasedLabels": [
          "released<%= nextRelease.channel ? ` on @${nextRelease.channel}` : '' %>"
        ],
        "addReleases": "top",
        "discussionCategoryName": "Announcements",
        "releaseNameTemplate": "<%= nextRelease.name %> 🚀",
        "releaseBodyTemplate": "## What's Changed\n\n<%= nextRelease.notes %>\n\n**Full Changelog**: https://github.com/magarcia/env-interpolation/compare/<%= lastRelease.gitTag %>...<%= nextRelease.gitTag %>"
      }
    ]
  ]
}
